export const DB_CONFIG = {
  HOST: process.env.POSTGRES_HOST,
  PORT: +process.env.POSTGRES_PORT,
  USER_NAME: process.env.POSTGRES_USER,
  PASSWORD: process.env.POSTGRES_PASSWORD,
  DB: process.env.POSTGRES_DB,
};
